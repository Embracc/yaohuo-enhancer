# 妖火网增强插件

面向 [妖火网](https://www.yaohuo.me) 的油猴 / VIA 用户脚本：列表浏览、分屏预览、评论增强、发帖 UBB/图床、等级查询、自动更新检测等。  
**无 `@grant` / 无 `@require`**，自带迷你 jQuery，兼容 VIA 等轻量环境。作者：**Embrace（ID:19299）**。

当前版本：**v0.9.174**

---

## 安装

**国内（推荐，无需代理）**

```
https://ghfast.top/https://raw.githubusercontent.com/Embracc/yaohuo-enhancer/main/yaohuo-enhancer.user.js
```

备选：

```
https://gh-proxy.com/https://raw.githubusercontent.com/Embracc/yaohuo-enhancer/main/yaohuo-enhancer.user.js
```

**GitHub raw（需可访问 GitHub）**

```
https://raw.githubusercontent.com/Embracc/yaohuo-enhancer/refs/heads/main/yaohuo-enhancer.user.js
```

安装后打开妖火任意页，左下角 **⚙** 进入设置。设置内也可手动检测更新、复制安装链接。

---

## 功能概览

| 分组 | 能力 |
|------|------|
| **浏览** | 新标签打开帖子、列表分屏预览（右侧 iframe，可调比例/边距）、列表时间显示 |
| **界面** | 浮动按钮（回顶 / Lv 等级 / 只看楼主）、楼主标签、发帖·回复 UBB 工具栏与图床/视频上传、按钮透明度调节 |
| **评论** | 进帖加载全部评论并楼中楼整理、复读机 +1 / 「复」复制评论、列表/评论自动加载更多、自动吃肉、图片点击放大 |
| **更新** | 自动检测（每次打开页面一次）、手动检测；国内多镜像 + 取最高版本号 |

说明：

- **Lv** 只查**当前登录用户**（昵称、ID、妖晶、经验、等级等），不是楼主。
- **复读机 +1 / 「复」复制评论**：在按钮上滑动会取消发送，减少误触。
- **VIA**：需双 `@match`（`yaohuo.me` / `*.yaohuo.me`），同源 XHR 自动带 Cookie，勿手动设 Cookie 头。

仓库：https://github.com/Embracc/yaohuo-enhancer

---

## 更新日志

按里程碑整理；细碎样式/选择器/日志类修补已合并进对应大版本，不再逐条罗列。

### v0.9.174 — 等级弹窗可点击跳转

- 👤 等级弹窗中**妖晶**可点击跳转至银行流水
- 👤 等级弹窗中**经验**可点击跳转至等级信息页

### v0.9.152 ~ v0.9.173 — 图片缩放、拖拽优化、楼主标签、滚动加载、设置美化

- 🖼️ 图片点击全屏放大，支持滚轮/双指缩放、拖拽平移（＞3px 视为拖动不触发关闭），取消过渡延迟实现丝滑跟手
- 🔁 复读机 +1 保留评论中的图片/表情（转为 `[img]` UBB）
- 🏷️ 新增**楼主标签**（评论中楼主自动显示绿色标签，可自定义颜色）
- 📜 帖子评论区滚动到底自动加载下一页（非加载全部模式）
- 🫸 分屏左侧边距偶发丢失修复：`<style>` 被页面移除后自动重建，每 2 秒兜底检查
- 🧹 完全移除只看楼主切换功能（嵌套回复保序无法彻底解决）
- 📥 设置开关文案精简

### v0.9.119 ~ v0.9.151 — 更新检测、齿轮拖动、分屏设置、时间显示

- 🔍 新增**手动检测更新** + **自动检测更新**（每次打开页面检测一次，不重复）
- 🌐 国内可检更新：多源并行检测（gh 代理 + jsDelivr 等），取最高 `@version`，避免 CDN 旧缓存误判
- ⚙ 设置齿轮可拖动并记住位置（localStorage），刷新/换页后位置保持
- 🎚️ 齿轮透明度滑块（5%～100%），同时作用于所有浮动按钮
- 📐 分屏设置组：左侧占比（20%～60%）、左侧边距（0～50px），拖拽滑块实时更新
- ⏱️ 列表页帖子标题旁显示相对时间（今天隐藏 / 昨天 1天前 / 更早 N天前）
- 🥩 自动吃肉：进入肉贴检查自己有无回复，无则自动回复（每天重置）
- 🔗 安装链接加 `?_=时间戳`，避免 CDN 缓存旧版
- 📋 复读机旁新增「复」按钮，写入回复框不发送；可设置开关控制

### v0.9.76 ~ v0.9.118 — 评论全量加载、楼中楼整理、等级查询

- 📥 进帖拉取全部评论后再整理楼中楼，分页只取 `page=` 避免错误 URL
- 👤 **Lv 等级查询**仅显示当前账号；弹窗含昵称、ID、妖晶/经验/等级/帖子/回复
- 🎛️ 设置面板分组对齐（浏览·界面·评论；界面子项缩进）
- 🔄 复读机：按钮上滑动不发送，降低浏览误触
- 🖥️ 列表分屏改为文档捕获委托，兼容动态加载行
- 🛠️ VIA / 迷你 jQuery / 上传遮罩等兼容与稳定性改进

### v0.9.0 ~ v0.9.75 — 起步阶段

- 📋 列表分屏预览（右侧 iframe 原页），分屏内交互与左侧回顶
- 🔄 复读机 +1 / 「复」复制评论 与样式选项
- 📝 回复/发帖 UBB 工具栏（插入、导入）
- 🖼️ 图片 / 视频上传（统一图床接口）
- 🥩 自动吃肉（每日限制，分屏 iframe 可用）
- 🤖 AI 总结 / 生成回复与标题（早期能力，后续精简）
- 🏠 初版：新标签打开、只看楼主、回顶、设置 checkbox 等

---

## 开发说明

- 主文件：`yaohuo-enhancer.user.js`
- 每次发版需同步：`@version`、设置面板版本号、控制台初始化日志、本 README
- 推送请同时包含脚本与 README，避免只推其一

## 许可

以仓库内声明为准；仅供学习与个人增强使用，请遵守妖火网规则。