# 妖火网增强插件

面向 [妖火网](https://www.yaohuo.me) 的油猴 / VIA 用户脚本：列表浏览、分屏预览、评论增强、发帖 UBB/图床、等级查询、自动更新检测等。  
**无 `@grant` / 无 `@require`**，自带迷你 jQuery，兼容 VIA 等轻量环境。作者：**Embrace（ID:19299）**。

当前版本：**v0.9.151**

---

## 安装

**国内（推荐，无需代理）**

```
https://ghfast.top/https://raw.githubusercontent.com/Embracc/yaohuo-enhancer/main/yaohuo-enhancer.user.js
```

备选：

```
https://gh-proxy.com/https://raw.githubusercontent.com/Embracc/yaohuo-enhancer/main/yaohuo-enhancer.user.js
```

**GitHub raw（需可访问 GitHub）**

```
https://raw.githubusercontent.com/Embracc/yaohuo-enhancer/refs/heads/main/yaohuo-enhancer.user.js
```

安装后打开妖火任意页，左下角 **⚙** 进入设置。设置内也可手动检测更新、复制安装链接。

---

## 功能概览

| 分组 | 能力 |
|------|------|
| **浏览** | 新标签打开帖子、列表分屏预览（右侧 iframe，可调比例/边距）、列表时间显示 |
| **界面** | 浮动按钮（回顶 / Lv 等级 / 只看楼主）、楼主标签、发帖·回复 UBB 工具栏与图床/视频上传、按钮透明度调节 |
| **评论** | 进帖加载全部评论并楼中楼整理、复读机 +1 / 「复」复制评论、列表/评论自动加载更多、自动吃肉、图片点击放大 |
| **更新** | 自动检测（每次打开页面一次）、手动检测；国内多镜像 + 取最高版本号 |

说明：

- **Lv** 只查**当前登录用户**（昵称、ID、妖晶、经验、等级等），不是楼主。
- **复读机 +1 / 「复」复制评论**：在按钮上滑动会取消发送，减少误触。
- **VIA**：需双 `@match`（`yaohuo.me` / `*.yaohuo.me`），同源 XHR 自动带 Cookie，勿手动设 Cookie 头。

仓库：https://github.com/Embracc/yaohuo-enhancer

---

## 更新日志

按里程碑整理；细碎样式/选择器/日志类修补已合并进对应大版本，不再逐条罗列。

### v0.9.149 ~ v0.9.151 — 图片点击放大与复读表情

- 🖼️ 帖子/评论中图片点击全屏放大，支持滚轮缩放、双指捏合、拖拽平移，再次点击关闭
- 🔁 复读机 +1 将评论中的图片转为 `[img]` UBB 代码，保留表情/贴图不丢失

### v0.9.148 — 安装链接防缓存

- 弹窗/设置面板中的安装链接加 `?_=时间戳`，避免 CDN 缓存旧版

### v0.9.144 ~ v0.9.147 — 分屏设置

- 新增**分屏设置组**（仅分屏开启时显示）：左侧占比（20%～60%）、左侧边距（0～50px），拖拽滑块实时更新
- 分屏面板添加绿色分割线

### v0.9.143 — 分屏隐藏齿轮

- 分屏右侧 iframe 不再显示设置齿轮

### v0.9.138 ~ v0.9.142 — 列表时间显示

- 列表页帖子标题旁显示相对时间（今天隐藏 / 昨天显示 1天前 / 更早显示 N天前）
- 分屏下左侧列表左右 padding 缩减

### v0.9.137 — 自动吃肉检查页面

- 进入肉贴先检查页面上自己有没有带「吃」「肉」的回复，有则跳过，无则自动回复（每天重置）

### v0.9.135 ~ v0.9.136 — 按钮透明度

- 设置新增齿轮透明度滑块（5%～100%），同时作用于所有浮动按钮（齿轮、↑、Lv、楼主）

### v0.9.130 ~ v0.9.134 — 设置齿轮拖动位置

- 齿轮可拖动并记住位置（localStorage），刷新/换页后仍在拖到的位置

### v0.9.129 — 复制评论按钮

- 设置项文案：**填入回复框** → **复制评论**（默认关，写入回复框不发送）

### v0.9.126 ~ v0.9.128

- 复读机旁新增「复」按钮，写入回复框不发送；改为设置项 **📋 复制评论** 控制显示

### v0.9.125

- 修复 Lv 等级查询：HTTP 200 仍报失败的问题（校验/解析加固，关键中文用 Unicode 转义）

### v0.9.123 ~ v0.9.124

- 国内可检更新：多源并行检测（国内 gh 代理 + jsDelivr 等），取最高 `@version`
- 设置 / 弹窗区分「国内安装」与「GitHub raw」

### v0.9.119 ~ v0.9.122 — 脚本内更新检测

- 设置内：**手动检测更新** + **自动检测更新**
- 自动策略：每次打开网页检测一次，同页不重复；多次打开则多次检测
- 手动检测先关闭设置面板再出结果，避免弹窗叠层
- 多镜像取最高版本，降低单一 CDN 旧缓存误判

### v0.9.113 ~ v0.9.118 — 等级与设置体验

- Lv 仅显示当前账号；弹窗含昵称、ID、妖晶/经验/等级/帖子/回复等
- UID 优先 `myuserid` / 首页「空间」/ 我的地盘，避免误用楼主 ID
- 设置面板分组对齐（浏览·界面·评论；界面子项缩进）
- 复读机：按钮上滑动不发送，降低浏览误触

### v0.9.76 ~ v0.9.112 — 评论全量加载与楼中楼（摘要）

- 进帖拉取全部评论后再整理楼中楼；分页只用 `page=`，避免 `lpage` 与错误 URL 构造
- 列表分屏改为文档捕获委托，兼容动态加载行
- 列表/首页设置齿轮强制可见；浮动按钮组稳定展示
- 大量 VIA / 迷你 jQuery / 上传遮罩等兼容与稳定性改进（细节已收敛，不单列）

### v0.9.19 ~ v0.9.22 — 自动吃肉

- 进入肉贴可自动回复吃肉类内容；按日限制；分屏 iframe 场景可用

### v0.9.6 ~ v0.9.18 — 发帖 UBB 与多媒体

- 回复/发帖 UBB 工具栏（插入、导入）
- 图片 / 视频上传（统一图床接口）
- 分屏与右下角按钮组（回顶、Lv、楼主）布局成型
- 等级查询初版（后续版本修正为「查自己」）

### v0.9.0 ~ v0.9.5 — AI 与 UBB 提示

- AI 总结 / 生成回复与标题（早期能力；当前构建以无外部依赖的本地增强为主，设置中已精简）
- UBB 回复代码提示与发帖页按钮适配

### v0.7.0 ~ v0.8.x — 分屏预览

- 列表点帖右侧分屏（iframe 原页）
- 分屏内交互与左侧回顶等体验完善

### v0.4.0 ~ v0.6.0 — 加载与复读

- 滚动自动加载更多
- 复读机 +1 / 「复」复制评论 与样式选项

### v0.1.0 ~ v0.3.0 — 起步

- 初版：新标签打开、楼层/回复相关增强、只看楼主、回顶等
- 精简为妖火实际 DOM 可用的核心能力；设置 checkbox 在站内 CSS 下可点

---

## 开发说明

- 主文件：`yaohuo-enhancer.user.js`
- 每次发版需同步：`@version`、设置面板版本号、控制台初始化日志、本 README
- 推送请同时包含脚本与 README，避免只推其一

## 许可

以仓库内声明为准；仅供学习与个人增强使用，请遵守妖火网规则。